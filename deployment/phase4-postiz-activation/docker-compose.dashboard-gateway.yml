services:
  dashboard:
    networks:
      gateway:
        ipv4_address: 172.30.252.4
        aliases:
          - tanaghom-integration-gateway

networks:
  gateway:
    external: true
    name: tanaghom-n8n-database-egress
