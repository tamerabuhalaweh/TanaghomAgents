---
name: execute-governed-ghl-action
description: Execute one database-authorized GoHighLevel action through Tanaghom's private gateway. Use only after policy, consent, approval, channel, idempotency, and emergency-stop checks pass.
---

# Execute Governed GHL Action

Accept only a server-prepared dispatch for the current organization and an explicitly allowed operation and channel.

Execute the single bounded provider action through the private integration gateway and return normalized evidence. Preserve idempotency; classify uncertain outcomes as indeterminate and stop.

Do not create new permissions, select additional recipients, change the requested operation, expose credentials, or retry an indeterminate action.
